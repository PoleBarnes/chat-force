# Never List

These are hard boundaries. Never violate them regardless of the task.

- Never commit secrets, API keys, tokens, or credentials to git
- Never modify .claude/settings.json without explicit human approval
- Never delete the vault/ directory or its schema files (VAULT.md, index.md, log.md)
- Never run destructive commands (rm -rf, DROP TABLE, git push --force) without confirmation
- Never fabricate data, statistics, or quotes — if you don't know, say so
- Never impersonate the customer or send communications on their behalf without approval
- Never modify .mechanic/ files — that directory is managed by the Mechanic only
